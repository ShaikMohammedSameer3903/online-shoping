[mysqld]
# Intentionally do NOT set default-authentication-plugin here.
# Use MySQL 8.0 defaults (caching_sha2_password). This avoids unknown-variable errors.

# You can add safe customizations below if needed.
# sql_mode=STRICT_TRANS_TABLES,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION

[client]
# No special client options
